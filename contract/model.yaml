  properties:
    a:
      type: number
      example: 5
      description: First operand
    b:
      type: number
      example: 3
      description: Second operand
    operation:
      type: string
      enum: ["+", "-", "*", "/"]
      example: "+"
      description: Operator